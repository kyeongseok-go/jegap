# HANDOFF — JEGAP 확장 설계서

작성: 2026-09-20 · 대상: 다음 세션(자동 루프 포함)
전제: **한 번에 한 단계씩**. 각 단계는 끝나면 빌드·테스트·배포·검증까지 마치고 커밋한다.
순서 원칙: **새 API 키 발급이 필요 없는 것부터**. 키가 필요한 단계는 사용자 승인/발급이 선행돼야 하므로 뒤로 뺀다.

---

## 0. 현재 상태 (2026-09-20 기준)

- 서비스: https://jegap-one.vercel.app (프로덕션 최신) · 저장소: https://github.com/kyeongseok-go/jegap (**공개 전환 완료**)
- 원티드 AI Championship 2026 제출 완료. 심사기간 09-21~10-17 **링크 상시 가용 필수** → 배포는 항상 green 유지.
- 구성: 관리비(K-apt 21,165단지) · 병원비(심평원 3,944기관) · 학원비(NEIS 21,038곳) · 장례비(e하늘 1,060곳) · 생활물가(행안부 35품목×17시도)
- 최근 반영: Codex 검토 대응(수치 해석 정정·모바일 파손 수정·출력 검증·보안 헤더), 히어로 문구 단축, 뒤로가기, 착한가격업소 주소 중복 제거
- 테스트 34개 · 빌드 37페이지 · 커밋 기준 `main`

### 보유 중인 키 (app/.env.local, 값은 절대 출력·커밋 금지)
`DATA_GO_KR_KEY` · `NEIS_KEY` · `ANTHROPIC_API_KEY` · (그 외 미사용: SUPABASE_*, NAVER_*)

### 인제스트별 키 필요 여부 (실측)
| 스크립트 | 키 |
|---|---|
| `ingest.py` (K-apt) | 불필요 (자료실 벌크) |
| `ingest_funeral.py` (e하늘) | 불필요 (공공누리 zip) |
| `ingest_living.py` (행안부) | 불필요 |
| `ingest_goodprice.py` | 불필요 |
| `ingest_hira.py` (심평원) | `DATA_GO_KR_KEY` (보유) |
| `ingest_neis.py` (NEIS) | `NEIS_KEY` (보유) |

---

## 1. 진행 순서

### STEP 1 — 학원·장례 비교 단위 보존 재수집 ★ 키 불필요/보유 키만 사용
**왜 먼저인가**: 검토 보고서(`docs/2026-09-17-UX-SECURITY-REVIEW.md` P0-1)의 **유일한 미해결 항목**이자, 새 데이터를 붙이기 전에 기존 데이터의 신뢰를 먼저 세워야 한다. 새 키도 필요 없다.

문제(재현됨):
- `app/scripts/ingest_neis.py:36` 키워드로 과목을 합침(피아노·바이올린→음악, 태권도·수영→체육). `:89`에서 원래 과정명 대신 표준 코드와 중앙값 저장. **시간·횟수·정원이 비교에 없음.**
- `app/scripts/ingest_funeral.py:40` 세부내용을 버리고 품명만 키로 사용. **규격·기간·구성(특실/일반실, 1일/1시간)이 배수에 섞일 수 있음.**

할 일:
1. 두 인제스트를 **원본 필드 보존** 방식으로 재작성. 과정명·학년·과목·시간·횟수·기간, 장례는 규격·단위·기간·포함 서비스를 그대로 저장.
2. **비교 가능성 판정 함수**를 도메인 어댑터에 추가: 단위·기간·구성이 일치하는 표본만 배수·백분위 산출. 불일치·판정 불가 항목은 **공시가격만 표시**하고 배수·순위·하이라이트에서 제외.
3. 화면: 제외된 항목은 "공시가격 참고(조건 상이로 순위 미산출)"로 라벨. `PriceCheckup`/`ExamTable`에 상태 구분 추가.
4. 테스트: 단위 불일치 표본이 배수 계산에서 빠지는지, 판정 불가 항목이 하이라이트에 안 뜨는지.

완료 기준: 조건이 다른 과정끼리 배수가 섞이지 않고, 화면 문구(`/a` "조건이 다른 과정끼리는 가격 순위로 비교할 수 없습니다")가 **실제 동작과 일치**한다.

---

### STEP 2 — 국가법령정보 API로 법 조항 실시간 검증 ☆ 신규 키 필요(법제처, 즉시 발급)
**왜 두 번째인가**: 대필 6종의 신뢰를 코드로 보증한다. 과거 적대 감사에서 **존재하지 않는 조항(시행령 §31⑩) 인용 사고**가 있었고, 현재는 수작업 대조에 의존한다.

- API: 국가법령정보 Open API (https://open.law.go.kr) — 현행 법령 목록·조문 조회
- 할 일: 빌드 타임(또는 주간 배치)에 `lib/engine/rx.ts`·`lib/pricedom/*`가 인용하는 **모든 조항 문자열을 실제 조문과 대조**하고, 불일치 시 빌드 실패 또는 해당 문서 비활성화. 어제 넣은 출력 검증(`polish.ts`의 `preservesFacts`)과 결합하면 "조항이 실제로 존재하는지까지 확인"이 된다.
- 주의: 조문은 개정된다. 검증 결과에 **대조 시점**을 남기고 `/method`에 표기.

---

### STEP 3 — 6호 도메인 신설 ☆ 신규 키 필요
아래 후보 중 **조사 결과가 확정된 것부터** 하나만 고른다. (조사 2건이 진행 중이었다 — 결과가 없으면 STEP 3 착수 전에 다시 조사할 것)

| 후보 | 4원칙 적합성 | 핵심 확인 사항 |
|---|---|---|
| **공정위 FairData 프랜차이즈** | ★★★ 가맹사업법이 정보공개서 제공을 **의무화** → "물어볼 권리가 법에 있음"을 유일하게 충족 | 창업비용(가입비·교육비·보증금·인테리어 평당 단가)이 브랜드별로 오는지 |
| **오피넷 주유소 가격** | ★★☆ 개별 주유소 단위·불투명·협상 불가. 단 "물어볼 권리"는 약하고 "고를 권리" 중심 | 개별 주유소 식별자·상호·주소·시계열 제공 여부, 상업적 이용 제한 문구 |
| **한국소비자원 참가격** | ★★☆ 신규 편보다 **생활물가편 해상도 업그레이드**(시도 평균 → 점포 단위)에 적합 | 판매점 단위인지 지역 평균인지 |
| **KAMIS 농수축산물** | ★★☆ 도매/소매 단계별 가격 → **"원재료는 내렸는데 완제품은 그대로"** 서사를 실데이터로 구현 | 같은 품목의 도매가·소매가를 함께 뽑을 수 있는지 |

선정 기준(헌법): ① 불투명 ② 개인이 협상 불가 ③ 물어볼 권리가 법에 있음 ④ 정부 데이터 존재. ③을 충족하면 대필(질의서) 기능이 성립하므로 **FairData 우선**.

착수 전 필수: 이용허락 조건(공공누리 유형)·일일 호출 한도·상업적 이용 제한을 **원문으로 확인**하고 `/terms`의 출처표에 추가.

---

### STEP 4 — 보류 항목 정리
- `docs/2026-09-17-REVIEW-RESPONSE.md`의 보류 3건: 출처별 이용허락 확인표(P1-16), 사용자 5명 테스트(미실시), CSP Report-Only → 정식 승격
- CSP는 위반 로그 확인 후 `Content-Security-Policy`로 승격. 폰트·이미지·스트리밍이 깨지면 안 된다.

---

## 2. 불변 제약 (헌법 — 매 단계 준수)

1. 무로그인, 클릭 1회 안에 첫 가치
2. **사실 화법** — 판정·등급 금지. 위치와 배수라는 사실만
3. **무소음** — 표본 부족·이상치·조건 불일치는 표시하지 않는다
4. **수치는 결정론 계산, AI는 문장만** — 서버가 숫자·조항 일치를 검증한 뒤에만 노출
5. 심사기간 링크 상시 가용 > 신기능
6. 없는 데이터를 추정하지 않는다. 확인 못 한 것은 "확인 불가"로 표기

---

## 3. 작업 절차 (매 단계 공통)

```bash
export PATH="$HOME/.local/node/bin:$PATH"
cd /Users/kogyeongseok/workspace/jegap/app
npx tsc --noEmit && npx vitest run && npm run build
vercel deploy --prod --yes        # git author가 kugll9606@naver.com 이어야 함
```

- 편집은 **assert 포함 python 스크립트**로 (무음 no-op 사고 3회 이력)
- 브랜치: 큰 변경은 `feat/<이름>`에서 작업 후 main 병합
- 로컬 확인: `npx next start -p 4000` (포트 점유 시 `lsof -ti:4000 | xargs kill -9`)
- 캡처: Chrome 헤드리스 `--window-size=1920,1080 --virtual-time-budget=6000` (모바일은 뷰포트 반영이 불안정 → 브라우저 도구 측정을 신뢰)

---

## 4. 함정 (반복 금지)

- **Vercel BLOCKED**: git 커밋 작성자 이메일이 Vercel 계정과 다르면 빌드가 스케줄되지 않고 status=UNKNOWN. 진단은 REST API `/v13/deployments/<url>`의 `readyStateReason`.
- **포트 4000 좀비 프로세스**가 구버전을 서빙해 "CSS가 깨졌다"로 오인한 적 있음. `lsof -ti:4000`로 정리 후 재기동.
- python3.14는 `SSL_CERT_FILE=/etc/ssl/cert.pem` 필요. pip는 `--user --break-system-packages`.
- JSX에서 텍스트 줄 다음 줄에 `<b>`가 오면 **공백이 사라진다** → `{" "}` 필요.
- API 키·환경변수 값을 출력하거나 커밋하지 말 것.

## 5. 참조

- 검토 보고서: `docs/2026-09-17-UX-SECURITY-REVIEW.md` / 대응·선정 근거: `docs/2026-09-17-REVIEW-RESPONSE.md`
- 설계문서: `artifacts/design-doc/index.html` (라이브 `/design`) · 아키텍처: `artifacts/jegap5.architecture.json`
- 제출 기재: `AI_TOOLS.md` · 한국 공공 API 목록: https://github.com/yybmion/public-apis-4Kr
